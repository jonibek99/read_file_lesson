#Rread file and convert to list
def read_file(filename):
    """
    Reads a file and returns a list of integers.
    Args:     filename (str): The name of the file to read.
    Returns:    data (list): A list of integers from the file.
    """
    return filename
a=open('data.txt','r').readline()
print(read_file(a))



